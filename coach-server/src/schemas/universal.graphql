type Items {
  metrics: [Metric!]
  goals: [Goal!]
  todos: [Todo!]
  routines: [Routine!]
}

type Type {
  id: Int
  text: String!
  altText: String
  description: String
}

input TypeInput {
  id: Int
  text: String
  altText: String
  description: String
}
