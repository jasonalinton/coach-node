mutation rescheduleIteration($id: Int!, $startAt: String!) {
  rescheduleIteration(id: $id, startAt: $startAt) {
    id
    text
    startAt
    endAt
    attemptedAt
    completedAt
    isRecommended
  }
}
